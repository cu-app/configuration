import type React from "react"
export const metadata = {
  title: "CU.APP Rule Builder - Visual Automation",
  description: "Build banking automation rules with a visual, node-based interface",
}

export default function RulesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
